import { InterceptorServicio } from './interceptor-servicio';

describe('InterceptorServicio', () => {
  it('should create an instance', () => {
    expect(new InterceptorServicio()).toBeTruthy();
  });
});
